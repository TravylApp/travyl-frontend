name: Expo Preview
on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  update:
    name: EAS Update
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          # eas-cli 18+ requires Node 20+. Was pinned to 18 here, which
          # made every push to develop fail with "engine node ... Expected
          # version >=20.0.0" while installing eas-cli@18.11.
          node-version: 20
          cache: 'npm'

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          eas-version: latest
          eas-cache: true
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: npm install

      - name: Publish Update
        run: npx eas update --branch preview --message "Auto-update from develop @ ${{ github.sha }}"
        working-directory: apps/mobile
