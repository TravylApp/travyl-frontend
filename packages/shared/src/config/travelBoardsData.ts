import type { TravelBoard } from '../types';

export const TRAVEL_BOARDS: TravelBoard[] = [];

export const TRAVEL_DNA: { label: string; pct: number; color: string }[] = [];

export const JOURNEY_NUMBERS: { icon: string; color: string; value: string; label: string }[] = [];
