/** Expo ease-out — fast start, smooth deceleration. Best for scroll-triggered entrances. */
// eslint-disable-next-line @typescript-eslint/no-magic-numbers -- Cubic bezier animation constants
export const EASE_OUT_EXPO = [0.16, 1, 0.3, 1] as const;
